#pragma once

#include <string>
#include <vector>

namespace utils
{

/**
 * @brief Get PSU inventory object path from config
 */
std::vector<std::string> getPSUInventoryPath();

} // namespace utils
